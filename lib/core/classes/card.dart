import 'card_class.dart';

class GameCard {
  CardClass cardClass;
  String title;
  String description;

  GameCard({
    required this.cardClass,
    required this.title,
    required this.description,
  });
}